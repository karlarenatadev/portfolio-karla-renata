name: GitHub Pages Disabled

on:
  workflow_dispatch:

jobs:
  notice:
    runs-on: ubuntu-latest
    steps:
      - name: Explain deployment change
        run: |
          echo "GitHub Pages foi desativado para este projeto."
          echo "O chatbot agora depende de uma funcao serverless e deve ser publicado em uma plataforma como a Vercel."
